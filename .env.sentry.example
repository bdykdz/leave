# Sentry Error Tracking Configuration
# Copy this to .env.local and fill in your Sentry details

# Required: Your Sentry DSN (Data Source Name)
# Get this from your Sentry project settings
NEXT_PUBLIC_SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id

# Optional: For uploading source maps (improves error reports)
SENTRY_ORG=your-organization-slug
SENTRY_PROJECT=your-project-slug
SENTRY_AUTH_TOKEN=your-auth-token

# Optional: Environment name (defaults to NODE_ENV)
SENTRY_ENVIRONMENT=production

# Optional: Trace sample rate (0.0 to 1.0)
# Lower values reduce performance impact
SENTRY_TRACES_SAMPLE_RATE=0.1

# Optional: Session replay sample rate (0.0 to 1.0)
# Records user sessions for debugging
SENTRY_REPLAY_SESSION_SAMPLE_RATE=0.1
SENTRY_REPLAY_ON_ERROR_SAMPLE_RATE=1.0